{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} -
    {{ city.label }}
{% endblock %}

{% block body %}
    <div class="container mt-3 text-center">
        <div class="row justify-content-center">
            <div class="col-md-8" id="grid-column">
                <div class="jumbotron p-0 ">
                    {# Jumbotron détail d'une ville#}
                    <div class="view overlay rounded-top">
                        <img src="{{ asset('uploads/cities/' ~ city.headerphoto) }}" class="img-fluid" alt="Sample image">
                        <div>
                            <div class="mask rgba-white-slight"></div>
                        </div>
                    </div>

                    <div class="card-body text-center mb-3">

                        <h3 class="card-title h3 my-4 global-title" ><strong>{{city.name}}</strong></h3>

                    </div>
                </div>
            </div>
        </div>
    </div>
    <main class="container">
        <h2 class="h2-responsive" style="margin-top: 20px" >Toutes les activités à {{  city.label }} ({{ city.activities|length }} {% if city.activities|length > 1 %} résultats{% else %} résultat{% endif %})</h2>

        {# Boucle sur toutes les activités qui concernent la ville #}
        <section class="row projects">
            {% for activity in city.activities |sort((a, b) => a.eventdate <=> b.eventdate) %}
                <div class="col-md-6 col-lg-4">
                    {% include 'activity/_card.html.twig' with {'activity': activity} only %}
                </div>
            {% endfor %}
        </section>

    </main>

{% endblock %}
